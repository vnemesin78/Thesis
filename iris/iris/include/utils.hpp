#ifndef _UTILS_HPP_
	#define _UTILS_HPP_
	
	/**@fn
	 * @param rep_name : rep. name
	 * @brief
	 * Check if the rep. exist.
	 * 
	 **/
	int is_dir( const char * rep_name ) ;
	
	
	int file_exist( const char * file_name );
	
	
	int create_file( const char * file_name );
#endif
